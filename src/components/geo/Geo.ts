import React, { Component, useEffect, useState } from 'react';

class Geo extends Component {
    const [location: Array<number>, setLocation: Array<number>] = useState('');
    
    useEffect(() => {
        const success = location => {
            
        }
    })

    render() {
        return (

        )
    }
}

export default Geo;
